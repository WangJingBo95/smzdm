server:
  port: 80
#  context-path: /toutiao
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/smzdm?serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  velocity:
    suffix: .html
    expose-session-attributes: true
    expose-request-attributes: true
    allow-request-override: true #允许覆盖model中相同名的Object
#    allow-session-override: true